---
name: Get Commit
route: /queries/get-commit
menu: Queries
---

import {
  Pagination,
  QueryViewer,
  QueryTab,
  VariablesTab
} from "../../src/components";

# Get Commit

This method can be used to get a commit object using the commit id

### API

```js
   getCommit(id, path) => Promise<Commit>
```

| param | type   | description                      |
| :---- | :----- | :------------------------------- |
| id    | string | Commit ID                        |
| path  | string | Path to the repository directory |

### Query

<QueryViewer>
  <QueryTab>
    {`query {
   getCommit($id: String!, path: String!) {
      getCommit(id: $id, path: $path) {
         message
         tree
         parent
         author {
            name
            email
            timestamp
         }
         committer {
            name
            email
            timestamp
         }
      }
   }
}
`}
  </QueryTab>
  <VariablesTab>
    {`{
   id: "5bf6efa456a358c4dd39993cd982c59b57952e0b"
   path: "AppName/data/EntityName"
}
`}
  </VariablesTab>
</QueryViewer>
<Pagination
prev={{ name: "Open File", link: "/queries/open-file" }}
next={{ name: "Get Commits", link: "/queries/get-commits" }}
/>
