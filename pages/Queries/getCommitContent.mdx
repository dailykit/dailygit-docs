---
name: Get Commit Content
route: /queries/get-commit-content
menu: Queries
---

import {
  Pagination,
  QueryViewer,
  QueryTab,
  VariablesTab
} from "../../src/components";

# Get Commit Content

This method can be used to get the content of the commit using commit id

### API

```js
   getCommitContent(id, path) => Promise<string>
```

| param | type   | description                   |
| :---- | :----- | :---------------------------- |
| id    | string | Commit ID                     |
| path  | string | Path where the file is stored |

### Query

<QueryViewer>
  <QueryTab>
    {`query getCommitContent($id: String!, $path: String!){
  getCommitContent(id: $id path: $path)
}
`}
  </QueryTab>
  <VariablesTab>
    {`{
   "id": "5bf6efa456a358c4dd39993cd982c59b57952e0b",
   "path": "appname/data/entityname/filename.json"
}
`}
  </VariablesTab>
</QueryViewer>
<Pagination
  prev={{ name: "Get Commit Log", link: "/queries/get-commit-log" }}
  next={{ name: "Create Folder", link: "/mutations/create-folder" }}
/>
