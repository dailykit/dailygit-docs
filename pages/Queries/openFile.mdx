---
name: Open File
route: /queries/open-file
menu: Queries
---

import {Pagination, QueryViewer, QueryTab, VariablesTab} from '../../src/components'

# Get open file

This method can be used to open a file from any app into file editor

### API

```js
   openFile(path) => Promise<File>
```

|param|type|description|
|:--|:--|:--|
|path|string|Path where the file is stored|

### Query
<QueryViewer>
   <QueryTab>
{`query openFile($path: String!){
  openFile(path: $path) {
    name
    path
    type
    content
    size
    createdAt
    commits
    lastSaved
  }
}
`}
   </QueryTab>
   <VariablesTab>
{`{
   "path": "../apps/appname/data/entityname/filename.json"
}
`}
   </VariablesTab>
</QueryViewer>

<Pagination prev={{name: "Get File", link: "/queries/get-file"}} next={{name: "Search Files", link: "/queries/search-files"}} />