---
name: Get Files
route: /queries/get-files
menu: Queries
---

import {Pagination, QueryViewer, QueryTab, VariablesTab} from '../../src/components'

# Get files

This method can be used to get all the files within a folder

### API

```js
   getFiles(path, offset, limit) => Promise<[File]>
```

|param|type|description|
|:--|:--|:--|
|path|string|Path to the folder to get the files inside|
|offset|number|The number from where to start the page|
|limit|number|Number of rows to fetch|

### Query
<QueryViewer>
   <QueryTab>
{`query getFiles($path: String!, $offset: Int!, $limit: Int!){
  getFiles(path: $path, offset: $offset, limit: $limit) {
    type
    size
    createdAt
    commits
    lastSaved
    path
    name
    content
  }
}
`}
   </QueryTab>
   <VariablesTab>
{`{
   "path": "./../apps/appname/data/entityname",
   "offset": 2,
   "limit":10
}
`}
   </VariablesTab>
</QueryViewer>

<Pagination prev={{name: "Get Nested Folders", link: "/queries/get-nested-folders"}} next={{name: "Get File", link: "/queries/get-file"}} />