---
name: Create Folder
route: /folder/create
menu: Folder
---

import {Pagination} from '../../src/components'

# Create Folder

This method can be used to create a new folder in the filesystem.

### API

```js
    createFolder(path) => Promise<string>
```

|param|type|description|
|:--|:--|:--|
|path|string|Path where to create the folder|

<Pagination prev={{name: "Rename File", link: "/file/rename"}} next={{name: "Folder - Delete Folder", link: "/folder/delete"}}/>
