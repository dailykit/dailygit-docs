---
name: Create File
route: /mutations/create-file
menu: Mutations
---

import {Pagination, QueryViewer, QueryTab, VariablesTab} from '../../src/components'

# Create File

This method can be used to create a new file in filesystem

### API

```js
   createFile(path, content) => Promise<Result>
```

|param|type|description|
|:--|:--|:--|
|path|string|Path where to store the file|
|content|string|Content to store in the file|

### Query
<QueryViewer>
   <QueryTab>
{`mutation createFile($path: String, $content: String) {
  createFile(path: $path, content: $content) {
    ... on Error {
      success
      error
    }
    ... on Success {
      success
      message
    }
  }
}
`}
   </QueryTab>
   <VariablesTab>
{`{
  "path": "./../apps/appname/data/entityname/foldername/filename.json",
  "content": "hello world!"
}
`}
   </VariablesTab>
</QueryViewer>

<Pagination prev={{name: "Rename Folder", link: "/mutations/rename-folder"}} next={{name: "Delete File", link: "/mutations/delete-file"}} />